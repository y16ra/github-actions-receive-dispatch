name: dispatch receiver

on:
  repository_dispatch:
    types: [point_core_release]

jobs:
  receive:
    runs-on: ubuntu-latest
    steps:
      - name: receive event info
        run: |
          echo "event name: ${{ github.event_name }}"
          echo "release version: ${{ github.event.client_payload.version }}"
